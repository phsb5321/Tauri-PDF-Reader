# actionlint config. Registers this repo's self-hosted runner labels so
# actionlint does not flag `vm103` (and friends) as unknown runner labels.
# The CI + Release workflows run on the self-hosted vm103 runner
# (GitHub-hosted runners are billing-locked for this account).
self-hosted-runner:
  labels:
    - vm103
