# actionlint config. Registers this repo's self-hosted runner labels so
# actionlint does not flag `vm103` and the repo-scoped M5 as unknown labels.
# Hosted runners are billing-locked; untrusted PR code never reaches the Mac.
self-hosted-runner:
  labels:
    - vm103
    - lectrice-mac
