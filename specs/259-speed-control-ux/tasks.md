# Tasks 259
- [x] T001. Receipt + parent-disabled semantics trace
      (`disabled={isPlaying || isPaused || isLoading}`; setSpeed
      catches/logs/resolves).
- [x] T002. Component: aria-valuetext, off-lattice-truthful format,
      disabled-reason derivation + hint, dimmed disabled value.
- [x] T003. CSS: focus ring, hover/pressed, reduced-motion, wrap guard,
      tabular-nums, hint style.
- [x] T004. `AiSpeedSlider.test.tsx` (6 authored-UNRUN cases).
- [x] T005. Specs + DELIVERY report; frozen.
- [ ] T006. GRANTED-SLOT (251): run the scoped test + typecheck +
      harness-status; measured failures → max 2 corrections.
- [ ] T007. OPEN: in-session speed (backend/clock work — explicit
      prerequisite); native journey coverage (coordinator gate).
