# Tasks — 257 page controls

- [x] Read COMMON.md + p18.md; verify locked worktree at `35801daa`.
- [x] Trace `usePageNavigation` + baseline `PageNavigation`; confirm double
      dispatch, parseInt acceptance, same-page re-fire, draft survival bugs.
- [x] Implement deliberate draft policy in `PageNavigation.tsx` (safe-integer
      full-string parse, single commit funnel, clamped-target guard, Escape
      cancel, store+document sync invalidation, exact a11y name restored).
- [x] Token-only CSS: focus-visible parity, reduced-motion, width, narrow clamp.
- [x] Scoped `PageNavigation.test.tsx` (12 scenarios incl. p16 corrective
      acceptance cases, signed-input policy mark, LECT-130 gap left unaccepted).
- [x] spec.md / plan.md / tasks.md (this set) + reports/DELIVERY.md.
- [ ] 251 serialized slot run of the scoped check (BLOCKED until grant).
- [ ] Exact-head Codex Sol review (GLM-authored), then lead-gated sequencing.
