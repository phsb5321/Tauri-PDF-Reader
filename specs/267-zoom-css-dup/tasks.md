# Tasks — 267-zoom-css-dup

- [x] T1 Fold `color-scheme: light` into the base `.zoom-select` block; remove the duplicate ruleset (css:S4666).
- [x] T2 Validate: 256 suite + ZoomControls suite, typecheck, lint.
- [x] T3 Land: push → required CI green → squash-merge → post-merge Sonar `new_violations = 0`.
